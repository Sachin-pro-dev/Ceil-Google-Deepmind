{
  "name": "ceil",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ceil - a live-replanning multi-agent orchestration runtime (Google iAPI + Managed Agents).",
  "engines": { "node": ">=20" },
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "migrate": "tsx scripts/migrate.ts",
    "demo:phase1": "tsx scripts/phase1-demo.ts",
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.2.17",
    "dotenv": "^16.4.7",
    "drizzle-orm": "^0.38.3",
    "fastify": "^5.2.0",
    "pino": "^9.6.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "drizzle-kit": "^0.30.2",
    "pino-pretty": "^13.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
